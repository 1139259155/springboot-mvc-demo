package com.pengbo.ssmdemo.mapper;

import org.springframework.stereotype.Service;

@Service
public interface UserMapper {

//    @Select(value = "select * from t_usr where id=#{id}")
//    User getUserById(String id);
}
